<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingVertical="12dp">

    <!-- Food Icon Circle -->
    <FrameLayout
        android:id="@+id/fl_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/bg_inventory_icon_circle"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/iv_food_icon"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_gravity="center"
            android:contentDescription="Food icon"
            android:src="@drawable/ic_egg"
            app:tint="@color/lime_primary" />
    </FrameLayout>

    <!-- Food Name -->
    <TextView
        android:id="@+id/tv_food_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:fontFamily="sans-serif"
        android:textColor="@color/on_surface"
        android:textSize="14sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@id/tv_food_expiry"
        app:layout_constraintEnd_toStartOf="@id/tv_food_quantity"
        app:layout_constraintStart_toEndOf="@id/fl_icon"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        tools:text="Organic Eggs (12pk)" />

    <!-- Expiry Info -->
    <TextView
        android:id="@+id/tv_food_expiry"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginTop="2dp"
        android:fontFamily="sans-serif"
        android:textColor="@color/on_surface_variant"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/tv_food_quantity"
        app:layout_constraintStart_toEndOf="@id/fl_icon"
        app:layout_constraintTop_toBottomOf="@id/tv_food_name"
        tools:text="Expires in 3 days" />

    <!-- Quantity -->
    <TextView
        android:id="@+id/tv_food_quantity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:fontFamily="sans-serif"
        android:textColor="@color/lime_primary"
        android:textSize="13sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/view_status_dot"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="6 left" />

    <!-- Status Dot -->
    <View
        android:id="@+id/view_status_dot"
        android:layout_width="8dp"
        android:layout_height="8dp"
        android:background="@drawable/bg_dot_lime"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
